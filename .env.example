# --- Core ---------------------------------------------------------------
# "development" (default) or "production". Production turns on secure cookies
# + https and requires the few vars below. On Render this is auto-detected.
APP_ENV=development

# Optional. If unset, a persistent key is generated and stored in the database
# on first boot, so you don't have to manage one. To pin your own:
# python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=

# --- Database -------------------------------------------------------------
# Leave unset locally to use SQLite (firstlight-dev.db in the project root).
# On Render this is injected from the managed Postgres database. REQUIRED in
# production (the app refuses to start on ephemeral SQLite there).
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# --- Email (REQUIRED in production) ----------------------------------------
# Prefer SMTP when SMTP_HOST + SMTP_USER + SMTP_PASSWORD are set.
# Otherwise the Brevo HTTP API is used when BREVO_API_KEY is an API key
# (xkeysib-…). If BREVO_API_KEY is an SMTP key (xsmtpsib-…), the app sends
# via smtp-relay.brevo.com automatically (set SMTP_USER to your Brevo login).

# Option A — Brevo SMTP (matches Brevo's "Welcome to SMTP" wizard):
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=                              # login shown in Brevo → SMTP & API
SMTP_PASSWORD=                          # SMTP key (xsmtpsib-…)
# Leave BREVO_API_KEY empty when using Option A, OR put the SMTP key there
# and set SMTP_USER (the app will route it to SMTP).

# Option B — Brevo HTTP API (no SMTP ports needed):
BREVO_API_KEY=                          # API key (xkeysib-…), not SMTP key

# The "From" address. Must be a sender you've verified with Brevo.
# Contact-form messages go to the admin account claimed at /setup.
MAIL_FROM="Bloom Anyway <hello@example.com>"

# --- Lemon Squeezy (optional; set when you wire up payments) ----------------
# Webhook secret: LS dashboard -> Settings -> Webhooks -> signing secret.
#   (Until set, incoming webhooks are rejected; the rest of the site works.)
# API key:        LS dashboard -> Settings -> API -> "+" (for the Sync button).
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_API_KEY=
